
export enum ComplaintStatus {
    OPEN = 'open',
    IN_PROGRESS = 'in-progress',
    ON_HOLD = 'on-hold',
    RESOLVED = 'resolved',
    CLOSED = 'closed',
    REJECTED = 'rejected',
    DUPLICATE = 'duplicate',
    INVALID = 'invalid',
}